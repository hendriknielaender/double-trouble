site:
  name: Double Trouble
  site: 'https://double-trouble.dev'
  base: '/'
  trailingSlash: false
  description: "🚀 Twice the fun, double the trouble."

  googleSiteVerificationId: orcPxI47GSa-cRvY11tUe6iGg2IO_RPvnA1q95iEM3M

# Default SEO metadata
metadata:
  title:
    default: Double Trouble
    template: '%s — Double Trouble'
  description: "🚀 Twice the fun, double the trouble."
  robots:
    index: true
    follow: true
  openGraph:
    site_name: Double Trouble
    images:
      - url: '~/assets/images/default.png'
        width: 1200
        height: 628
    type: website
  twitter:
    handle: '@doubletrblblogs'
    site: '@doubletrblblogs'
    cardType: summary_large_image

i18n:
  language: en
  textDirection: ltr

apps:
  blog:
    isEnabled: true
    postsPerPage: 4

    post:
      isEnabled: true
      permalink: '/post/%slug%' # Variables: %slug%, %year%, %month%, %day%, %hour%, %minute%, %second%, %category%
      robots:
        index: true

    list:
      isEnabled: true
      pathname: 'blog' # Blog main path, you can change this to "articles" (/articles)
      robots:
        index: true

    category:
      isEnabled: false
      pathname: 'category' # Category main path /category/some-category, you can change this to "group" (/group/some-category)
      robots:
        index: true

    tag:
      isEnabled: true
      pathname: 'tag' # Tag main path /tag/some-tag, you can change this to "topics" (/topics/some-category)
      robots:
        index: false

    isRelatedPostsEnabled: true
    relatedPostsCount: 4


ui:
  theme: 'system' # Values: "system" | "light" | "dark" | "light:only" | "dark:only"
