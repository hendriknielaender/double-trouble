---
import Item from "~/components/blog/GridItem.astro";

const { posts } = Astro.props;
---

<div class="grid gap-3 row-gap-3 md:grid-cols-2 lg:grid-cols-4 mb-12">
  {posts.map((post) => <Item post={post} />)}
</div>
