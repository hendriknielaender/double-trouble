---
import { getPermalink } from "~/utils/permalinks";

const { tags, enableTransition = false, class: className = "text-sm" } = Astro.props;
---

{
  tags && Array.isArray(tags) && (
    <ul class={className}>
      {tags.map((tag) => (
        <li class="light:bg-gray-100 bg-slate-800 inline-block mr-2 mb-2 py-0.5 px-2 rounded-lg border-2 light:border-grey-100 border-slate-600 light:hover:border-blue-500 hover:border-blue-500">
          <a transition:name={enableTransition ? "tag"+tag : undefined} href={getPermalink(tag, "tag")}>{tag} 🏷</a>
        </li>
      ))}
    </ul>
  )
}
